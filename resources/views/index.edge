<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8" />
    <title>Login</title>
    @include('default_include')
</head>

<body>
    <section class="section">
        <div class="container">
            <div class="logo"></div>
            <div class="subtitle">
                @loggedIn
                <h2> Olá {{ auth.user.username }} Você já está logado. Redirecionando</h2>
                <meta http-equiv="refresh" content="1; URL='/dashboard'" />
                @else
                <div class="loginBox">
                    <form action="/login" method="POST">
                        {{ csrfField() }}
                        <section class="section">
                            <div class="container has-text-centered">
                                <div class="box">
                                    <div class="title">
                                        lgpdSystem
                                    </div>
                                    <p class="subtitle">Você não está logado. Por favor realize o login no sistema</p>
                                    <div class="columns is-centered">
                                        <div class="column is-4 is-4-desktop">
                                            <div class="field">
                                                <div class="control">
                                                    <input class="input" type="email" name="email" placeholder="Email">
                                                </div>
                                            </div>
                                            <div class="field">
                                                <div class="control">
                                                    <input class="input" type="password" name="password"
                                                        placeholder="Senha">
                                                </div>
                                            </div>
                                            <div class="field">
                                                <button
                                                    class="button is-primary is-focused is-medium is-fullwidth">Login</button>
                                            </div>
                    </form>
                    <div class="container is-size-7 is-black is-small has-text-weight-bold">
                        <a class="button" href="/esqueciMinhaSenha">Esqueci minha senha</a>
                    </div>
                    <div class="column is-fullwidth">
                        <br>
                        @if(old('notification'))
                        <div class="notification is-failure">
                            {{ old('notification') }}
                        </div>
                        @endif
                        @if(old('error'))
                        <div class="notification is-danger">
                            {{ flashMessage('error') }}
                        </div>
                        @endif
                    </div>
                </div>

            </div>
        </div>
        </div>
    </section>
    </form>
    </div>
    @endloggedIn
    </div>
    </div>
    </section>
</body>

</html>